/**
 * Internal-only components (no shared semantic counterpart).
 * Add brand / product chrome here when Contimiti, Bull Board, etc. need
 * primitives that must not appear in Valenstonic.
 */

export {};
