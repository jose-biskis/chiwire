{
  "name": "@chiwire/hello-http",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal hello world HTTP service for Docker deployment testing.",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "typecheck": "tsc --build --pretty false"
  }
}
